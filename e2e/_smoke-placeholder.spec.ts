import { test } from "@playwright/test";

// Placeholder so the `e2e/` directory commits before real tests exist.
// The harness needs at least one spec for `playwright test --list` to
// resolve cleanly. Replace/expand this when actual specs land.
test.skip("placeholder", () => {});
